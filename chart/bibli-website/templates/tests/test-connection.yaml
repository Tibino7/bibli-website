apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bibli-website.fullname" . }}-test-connection"
  labels:
    {{- include "bibli-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bibli-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
